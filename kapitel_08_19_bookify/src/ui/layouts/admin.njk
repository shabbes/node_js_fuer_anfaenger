<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Bookify Administration{% endblock %}</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@2.1.1/css/pico.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@picocss/pico@2.1.1/css/pico.colors.min.css">

    <style>
        header {
            margin-bottom: 2rem;
        }

        main {
            display: flex;
            gap: 2rem;
        }

        aside {
            min-width: 200px;
            background: var(--pico-color-slate-100);
            padding: 1rem;
            border-radius: var(--pico-border-radius);
        }

        section {
            flex: 1;
        }

        {% block stylesheet %}{% endblock %}
    </style>
</head>
<body>
    <header class="container">
        <hgroup>
            <h1>Admin Dashboard</h1>
            <p>Verwalten Sie Ihre Bookify-Plattform</p>
        </hgroup>
        {% include "partials:admin_navigation.njk" %}
    </header>

    <main class="container">
        <aside>
            <nav>
                <ul>
                    <li><a href="/admin/users">Benutzerverwaltung</a></li>
                    <li><a href="/admin/books">Buchverwaltung</a></li>
                    <li><a href="/admin/system">Systeminformationen</a></li>
                </ul>
            </nav>
        </aside>
        <section>
            {% block content %}{% endblock %}
        </section>
    </main>

    {% include "partials:footer.njk" %}
</body>
</html>